var productCard = document.getElementById('col-product-card-<%= @product.id %>');
productCard.parentNode.removeChild(productCard);

var nbOfProducts = document.querySelector('.nb-of-products');
nbOfProducts.innerHTML = 'Il y a <strong><%= Product.count %></strong> categories'
